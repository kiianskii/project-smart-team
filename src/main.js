function ShowModal() {
  const elem = document.getElementById('menu-modal');
  elem.classList.add('is-open');
}

function CloseModal() {
  const elem = document.getElementById('menu-modal');
  elem.classList.remove('is-open');
}
